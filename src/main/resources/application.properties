# 1. Configuração de Fonte de Dados do Neon (Corrigida: Sem aspas simples)
# A URL deve estar em uma única linha contínua.
spring.datasource.url=postgresql://neondb_owner:npg_f6pOonXbjkM2@ep-wispy-thunder-a4o6p0fa-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# 2. As credenciais NÃO são necessárias, pois já estão na URL.

# 3. Força o Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# 4. Configuração JPA (Simplificada para testes)
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 5. Plataforma de banco de dados (Asegura o mapeamento correto do PostgreSQL)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect